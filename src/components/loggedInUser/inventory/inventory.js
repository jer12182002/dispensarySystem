import React, { Component, PropTypes } from 'react';
import './inventory.scss';

class inventory extends Component {
    render() {
        return (
            <div className="inventory-wrapper">inventory</div>
        );
    }
}

export default inventory;
